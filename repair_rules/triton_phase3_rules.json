[
  {
    "id": "phase3_benchmark_failure",
    "match": [
      {
        "source": "phase3_output",
        "contains": "failed with exit code"
      },
      {
        "source": "phase3_output",
        "contains": "Traceback"
      }
    ],
    "problem": "The generated module passed earlier checks but failed during TritonBench-T performance benchmarking.",
    "fix": "Repair benchmark/runtime issues while preserving the required wrapper signature, Phase 1 callability, and Phase 2 semantics. Prefer robust shape handling and valid Triton launch parameters over narrow test-case assumptions.",
    "avoid": [
      "Do not optimize by changing the wrapper signature or return type.",
      "Do not remove functionality needed for Phase 2 correctness.",
      "Do not assume Phase 3 failures mean the code is semantically correct for all benchmark shapes."
    ]
  }
]
